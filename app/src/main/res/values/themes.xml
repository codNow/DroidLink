<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.DroidLink" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primaryDark</item>
        <item name="colorSecondaryVariant">@color/primary</item>
        <item name="colorOnSecondary">@color/primary</item>
        <!-- Status bar color. -->
        <item name="android:textColorSecondary">@color/theme_color</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:navigationBarColor">@color/primary</item>
        <item name="android:screenOrientation">portrait</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="CustomSwitchStyle" parent="Widget.MaterialComponents.CompoundButton.Switch">
        <!-- Checked state color -->
        <item name="colorSecondary">@color/switchCheckedColor</item>
        <!-- Unchecked state color -->
        <item name="android:colorForeground">@color/switchUncheckedColor</item>
    </style>
</resources>